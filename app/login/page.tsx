'use client';
import Nav from '@/components/Nav';import { useState } from 'react';import { useRouter } from 'next/navigation';import { signIn,hasSupabase } from '@/lib/supabase';
export default function Login(){const r=useRouter();const [email,setEmail]=useState('demo@watchvault.it');const [password,setPassword]=useState('demo1234');const [msg,setMsg]=useState('');async function submit(e:React.FormEvent){e.preventDefault();if(!hasSupabase){r.push('/dashboard');return}try{await signIn(email,password);r.push('/dashboard')}catch(err){setMsg(err instanceof Error?err.message:'Accesso non riuscito')}}return <><Nav/><form className="form-wrap" onSubmit={submit}><div className="eyebrow">BENTORNATO</div><h1>Accedi a WatchVault</h1><p className="muted">Senza Supabase configurato, il pulsante apre la modalità demo.</p><div className="field"><label>Email</label><input value={email} onChange={e=>setEmail(e.target.value)} type="email" required/></div><div className="field"><label>Password</label><input value={password} onChange={e=>setPassword(e.target.value)} type="password" required/></div>{msg&&<p style={{color:'var(--red)'}}>{msg}</p>}<button className="btn primary" style={{width:'100%'}}>Accedi</button></form></>}
