'use client';
import Nav from '@/components/Nav';
import {useState} from 'react';
import {useRouter} from 'next/navigation';
import {signIn,hasSupabase} from '@/lib/supabase';
export default function Login(){const r=useRouter();const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[msg,setMsg]=useState('');const[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setMsg('');if(!hasSupabase){setMsg('Configurazione incompleta: mancano le variabili Supabase su Render.');return}setBusy(true);try{await signIn(email,password);r.replace('/dashboard')}catch(err){setMsg(err instanceof Error?err.message:'Accesso non riuscito')}finally{setBusy(false)}}return <><Nav/><form className="form-wrap" onSubmit={submit}><div className="eyebrow">BENTORNATO</div><h1>Accedi a WatchVault</h1><p className="muted">Accedi al tuo account reale. La modalità demo è stata rimossa.</p><div className="field"><label>Email</label><input value={email} onChange={e=>setEmail(e.target.value)} type="email" autoComplete="email" required/></div><div className="field"><label>Password</label><input value={password} onChange={e=>setPassword(e.target.value)} type="password" autoComplete="current-password" required/></div>{msg&&<p className="form-message">{msg}</p>}<button className="btn primary" style={{width:'100%'}} disabled={busy}>{busy?'Accesso…':'Accedi'}</button></form></>}
