'use client';
import AppShell from '@/components/AppShell';import { useEffect,useState } from 'react';import { getWatches,saveWatches } from '@/lib/storage';import { Watch } from '@/lib/types';
const blank:Watch={id:'',brand:'',model:'',reference:'',year:'',movement:'Automatico',caliber:'',diameter:'',material:'Acciaio',dial:'',waterResistance:'',purchasePrice:0,currentValue:0,condition:'Ottimo',status:'In collezione',image:'https://images.unsplash.com/photo-1524592094714-0f0654e20314?auto=format&fit=crop&w=1000&q=80',serial:'',notes:''};
export default function Collection(){const [items,setItems]=useState<Watch[]>([]);const [show,setShow]=useState(false);const [form,setForm]=useState(blank);useEffect(()=>setItems(getWatches()),[]);function add(e:React.FormEvent){e.preventDefault();const n=[{...form,id:crypto.randomUUID()},...items];setItems(n);saveWatches(n);setShow(false);setForm(blank)}return <AppShell><div className="pagehead"><div><div className="eyebrow">ARCHIVIO PERSONALE</div><h1>La mia collezione</h1></div><button className="btn primary" onClick={()=>setShow(!show)}>+ Aggiungi orologio</button></div>{show&&<form className="card" onSubmit={add} style={{marginBottom:24}}><div className="grid"><div className="field"><label>Marca</label><input required value={form.brand} onChange={e=>setForm({...form,brand:e.target.value})}/></div><div className="field"><label>Modello</label><input required value={form.model} onChange={e=>setForm({...form,model:e.target.value})}/></div><div className="field"><label>Referenza</label><input value={form.reference} onChange={e=>setForm({...form,reference:e.target.value})}/></div><div className="field"><label>Anno</label><input value={form.year} onChange={e=>setForm({...form,year:e.target.value})}/></div><div className="field"><label>Movimento</label><select value={form.movement} onChange={e=>setForm({...form,movement:e.target.value})}><option>Automatico</option><option>Manuale</option><option>Quarzo</option><option>Solare</option><option>Kinetic</option><option>Spring Drive</option></select></div><div className="field"><label>Calibro</label><input value={form.caliber} onChange={e=>setForm({...form,caliber:e.target.value})}/></div><div className="field"><label>Prezzo acquisto</label><input type="number" value={form.purchasePrice} onChange={e=>setForm({...form,purchasePrice:Number(e.target.value)})}/></div><div className="field"><label>Valore attuale</label><input type="number" value={form.currentValue} onChange={e=>setForm({...form,currentValue:Number(e.target.value)})}/></div><div className="field"><label>URL foto</label><input value={form.image} onChange={e=>setForm({...form,image:e.target.value})}/></div></div><button className="btn primary">Salva scheda</button></form>}<div className="watch-grid">{items.map(w=><article className="watch-card" key={w.id}><img src={w.image} alt={w.model}/><div className="watch-card-body"><span className="muted">{w.brand}</span><h3>{w.model}</h3><p className="muted">{w.reference}</p><div className="tags"><span className="tag">{w.movement}</span><span className="tag">{w.year}</span><span className="tag">{w.condition}</span></div><p className="value">€ {w.currentValue.toLocaleString('it-IT')}</p></div></article>)}</div></AppShell>}
