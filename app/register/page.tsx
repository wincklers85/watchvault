'use client';
import Nav from '@/components/Nav';import { useState } from 'react';import { useRouter } from 'next/navigation';import { signUp,hasSupabase } from '@/lib/supabase';
export default function Register(){const r=useRouter();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [username,setUsername]=useState('');const [msg,setMsg]=useState('');async function submit(e:React.FormEvent){e.preventDefault();if(!hasSupabase){r.push('/dashboard');return}try{await signUp(email,password,username);setMsg('Registrazione completata. Controlla la tua email.')}catch(err){setMsg(err instanceof Error?err.message:'Registrazione non riuscita')}}return <><Nav/><form className="form-wrap" onSubmit={submit}><div className="eyebrow">ENTRA NELLA COMMUNITY</div><h1>Crea il tuo account</h1><div className="field"><label>Nickname pubblico</label><input value={username} onChange={e=>setUsername(e.target.value)} required/></div><div className="field"><label>Email</label><input value={email} onChange={e=>setEmail(e.target.value)} type="email" required/></div><div className="field"><label>Password</label><input value={password} onChange={e=>setPassword(e.target.value)} type="password" minLength={8} required/></div>{msg&&<p>{msg}</p>}<button className="btn primary" style={{width:'100%'}}>Registrati</button></form></>}
