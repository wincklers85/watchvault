'use client';
import Nav from '@/components/Nav';
import LanguageSelector from '@/components/LanguageSelector';
import Link from 'next/link';
import {useState} from 'react';
import {useRouter} from 'next/navigation';
import {signUp,hasSupabase} from '@/lib/supabase';
export default function Register(){const r=useRouter();const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[username,setUsername]=useState('');const[terms,setTerms]=useState(false);const[marketing,setMarketing]=useState(false);const[msg,setMsg]=useState('');const[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setMsg('');if(!terms){setMsg('Per creare un account devi accettare i Termini e confermare di aver letto la Privacy Policy.');return}if(!hasSupabase){setMsg('Configurazione incompleta: mancano le variabili Supabase su Render.');return}setBusy(true);try{const data=await signUp(email,password,username,{termsVersion:'2026-08-08-dev-1',privacyVersion:'2026-08-08-dev-1',marketingConsent:marketing});if(data.access_token)r.replace('/dashboard');else setMsg('Registrazione completata. Controlla la tua email e conferma l’account, poi accedi.')}catch(err){setMsg(err instanceof Error?err.message:'Registrazione non riuscita')}finally{setBusy(false)}}return <><Nav/><form className="form-wrap" onSubmit={submit}><div className="register-top"><div><div className="eyebrow">ENTRA NELLA COMMUNITY</div><h1>Crea il tuo account</h1></div><LanguageSelector/></div><div className="field"><label>Nickname pubblico</label><input value={username} onChange={e=>setUsername(e.target.value)} minLength={3} required/></div><div className="field"><label>Email</label><input value={email} onChange={e=>setEmail(e.target.value)} type="email" autoComplete="email" required/></div><div className="field"><label>Password</label><input value={password} onChange={e=>setPassword(e.target.value)} type="password" autoComplete="new-password" minLength={8} required/></div><label className="consent-row"><input type="checkbox" checked={terms} onChange={e=>setTerms(e.target.checked)} required/><span>Accetto i <Link href="/terms" target="_blank">Termini e condizioni</Link> e confermo di aver letto l'<Link href="/privacy" target="_blank">Informativa sulla privacy</Link>.</span></label><label className="consent-row optional"><input type="checkbox" checked={marketing} onChange={e=>setMarketing(e.target.checked)}/><span>Desidero ricevere, in futuro, novità e comunicazioni WatchVault. Facoltativo e revocabile.</span></label>{msg&&<p className="form-message">{msg}</p>}<button className="btn primary" style={{width:'100%'}} disabled={busy}>{busy?'Creazione account…':'Registrati'}</button><p className="legal-mini">WatchVault registra nel profilo la versione delle condizioni accettate e la preferenza marketing. Il consenso facoltativo non è preselezionato.</p></form></>}
