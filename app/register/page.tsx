'use client';
import Nav from '@/components/Nav';
import {useState} from 'react';
import {useRouter} from 'next/navigation';
import {signUp,hasSupabase} from '@/lib/supabase';
export default function Register(){const r=useRouter();const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[username,setUsername]=useState('');const[msg,setMsg]=useState('');const[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setMsg('');if(!hasSupabase){setMsg('Configurazione incompleta: mancano le variabili Supabase su Render.');return}setBusy(true);try{const data=await signUp(email,password,username);if(data.access_token)r.replace('/dashboard');else setMsg('Registrazione completata. Controlla la tua email e conferma l’account, poi accedi.')}catch(err){setMsg(err instanceof Error?err.message:'Registrazione non riuscita')}finally{setBusy(false)}}return <><Nav/><form className="form-wrap" onSubmit={submit}><div className="eyebrow">ENTRA NELLA COMMUNITY</div><h1>Crea il tuo account</h1><div className="field"><label>Nickname pubblico</label><input value={username} onChange={e=>setUsername(e.target.value)} minLength={3} required/></div><div className="field"><label>Email</label><input value={email} onChange={e=>setEmail(e.target.value)} type="email" autoComplete="email" required/></div><div className="field"><label>Password</label><input value={password} onChange={e=>setPassword(e.target.value)} type="password" autoComplete="new-password" minLength={8} required/></div>{msg&&<p className="form-message">{msg}</p>}<button className="btn primary" style={{width:'100%'}} disabled={busy}>{busy?'Creazione account…':'Registrati'}</button></form></>}
