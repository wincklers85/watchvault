# Database summary

Connected Supabase project: `wrzazkgwsldtvsgztgbl`.

Validation snapshot after V2 bootstrap:

- 32 tables in `public`
- 0 public tables without RLS
- 57 RLS policies in `public`
- 5 Storage buckets (`avatars`, `watches`, `marketplace`, `posts`, `documents`)
- `documents` is private
- Supabase Security Advisor: 0 findings after fixing the mutable function search path warning

This snapshot documents the remote state used while developing `feature/watchvault-supabase`.
