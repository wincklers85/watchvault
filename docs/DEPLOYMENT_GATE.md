# Deployment gate

Do not merge this branch into `main` until the V2 migration is exported and the production build plus RLS smoke tests pass. Use a Render preview/temporary service or feature-branch deploy first; keep the current production service on `main` until validation is complete.
